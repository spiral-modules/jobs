jobs:
  workers:
    command: "php handler.php"
    pool:
      numWorkers: 1
      maxJobs:    1

  # list of broker pipelines associated with endpoints
  pipelines:
    local:
      broker: local
      listen: true
      handle:
        - "spiral.jobs.tests.fixtures.local*"