rpc:
  enable: true
  listen: tcp://0.0.0.0:6001

jobs:
  enable: true

  # local rr endpoint configuration
  workers:
    command: "php tests/handler.php"
    pool:
      numWorkers: 1
      maxJobs: 1

  # list of endpoint pipelines associated with endpoints
  pipelines:
    default:
      endpoint: local
      options: []

  # list of active endpoints and endpoint consumers
  endpoints:
    local:
      enable: true